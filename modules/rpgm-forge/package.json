{
	"name": "rpgm-forge",
	"version": "2.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite build --watch & vite",
		"preview": "vite build --watch & vite preview",
		"build": "vue-tsc && vite build"
	}
}
