{
	"name": "rpgm-forge",
	"version": "1.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite build --watch & vite",
		"preview": "vite build --watch & vite preview",
		"lint": "vue-tsc; eslint; echo 'Linting complete. Above are any errors.'",
		"lint:fix": "vue-tsc; eslint --fix; echo 'Linting complete. Above are any errors.'",
		"build": "vue-tsc && vite build"
	}
}
